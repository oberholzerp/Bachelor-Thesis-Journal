submitted job on euler to get around the problem that 'make kernel' invokes the installation in /nfs/.../tgrosser/xilinx
 - changed path in commands generated by cmake and wrote them in an own script

iteration related work
 - microsoft catapult
 - added correct acronyms to the related work section

debugging xocc on euler
 - clang failure
 - LLVM IO failure - not enough space available

copied xilinx installation to scratch space from new
 - installation is recognized in scratch space

